<div class="container-fluid">
	<div id="page" class="row">

		<div class="col-sm-12 span12 details">
		{% if page.handle == 'artists'%}
			{% include 'artist-listing' %}
		{% else %}

		<h1 class="p25">{{ page.title }}</h1>
		{{ page.content }}
		
		{% endif %}  
		</div>
	</div>
</div>